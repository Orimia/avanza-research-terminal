# avanza-research-terminal — runtime dependencies
# Python 3.11+

streamlit>=1.33
pandas>=2.1
numpy>=1.26
plotly>=5.20
pydantic>=2.6
PyYAML>=6.0
requests>=2.31
httpx>=0.27
python-dotenv>=1.0
feedparser>=6.0          # RSS news / earnings calendars
APScheduler>=3.10        # always-on engine scheduler (market-hours jobs)
tzdata>=2024.1           # timezone data (zoneinfo on slim/Docker images)

# --- Optional fallback price source (never sole source of truth) ---
yfinance>=0.2.40         # optional; guarded import
# pandas-datareader>=0.10  # optional Stooq fallback

# --- Optional LLM memo generation (deterministic engine works without it) ---
anthropic>=0.40          # optional; only used if ANTHROPIC_API_KEY is set

# --- Dev / test ---
pytest>=8.0
